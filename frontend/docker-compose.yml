services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tumlumtala-frontend
    env_file: .env
    ports:
      - "80:80"
    networks:
      - tumlumtala-net
    restart: unless-stopped

networks:
  tumlumtala-net:
    external: true
